{
  "name": "ipare",
  "private": true,
  "scripts": {
    "build": "lerna run build && sh scripts/prerelease.sh",
    "pretest": "npm run build",
    "test": "jest",
    "test-q": "ts-node scripts/copy-jest-config.ts && jest",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^18.6.1",
    "codecov": "^3.8.3",
    "jest": "^28.1.3",
    "lerna": "5.2.0",
    "ts-jest": "^28.0.7",
    "ts-node": "10.9.1",
    "typescript": "^4.7.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
